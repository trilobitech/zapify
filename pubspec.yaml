name: zapify
description: A new Flutter project.
version: 2.2.1+14
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: 3.22.2

dependencies:
  flutter:
    sdk: flutter

  # local packages
  analytics_core:
    path: "packages/core/analytics"
  banner_core:
    path: "packages/core/banner"
  config:
    path: "packages/config"
  config_manager_core:
    path: "packages/core/config_manager"
  di_core:
    path: "packages/core/di"
  domain_core:
    path: "packages/core/domain"
  ext:
    path: "packages/core/ext"
  flavor_main:
    path: "packages/flavor/main"
  resources:
    path: "packages/resources"

  # personal flutter packages
  error_adapter: ^0.1.0
  logify: ^1.0.0
  simple_nav: ^0.1.0
  state_action_bloc: ^0.1.0

  # third party packages
  app_install_date: ^0.1.1
  call_log: ^4.0.0
  collection: ^1.16.0
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  firebase_core: ^3.2.0
  firebase_crashlytics: ^4.0.3
  firebase_performance: ^0.10.0+3
  firebase_remote_config: ^5.0.3
  flutter_svg: ^1.1.6
  freezed_annotation: ^2.2.0
  get_it: ^7.2.0
  google_fonts: ^4.0.4
  google_mobile_ads: ^2.3.0
  in_app_review: ^2.0.9
  intl: ^0.19.0
  logging: ^1.1.1
  meta: ^1.8.0
  path: ^1.8.0
  permission_handler: ^10.2.0
  phone_number: ^1.0.0
  provider: ^6.0.5
  receive_intent: ^0.2.3
  rxdart: ^0.27.7
  shared_preferences: ^2.0.15
  shimmer: ^2.0.0
  sqlbrite: ^2.5.0
  sqflite: ^2.2.8+4
  stack_trace: ^1.10.0
  timeago: ^3.2.2
  timeago_flutter: ^1.1.0
  url_launcher: ^6.1.10


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.3.0
  flutter_lints: ^2.0.1
  freezed: ^2.2.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
