import 'package:flutter/material.dart';

class AppInfo {
  const AppInfo({
    required this.icon,
    required this.label,
    required this.brandColor,
  });

  final IconData icon;
  final String label;
  final Color brandColor;
}
